wireguard:
  interface: wg-interconnect
  port: 51899
  # private_key: "" # Must be provided by user, not generated by ansible
  ipv4_cidr: "10.10.0.0/16"
  ipv6_cidr: "fd00:10:10::/64"
  topology: mesh # mesh | hub-spoke
  keepalive: 25
  nodes: {}
